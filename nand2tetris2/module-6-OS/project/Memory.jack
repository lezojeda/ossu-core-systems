class Memory {
	static Array ram;
	static Array heap;

	function void init() {
		let ram = 0; // this gives us access to the complete ram because accessing ram[i] is accessing ram[0 + i]
		let heap = 2048;
		let freeList = 2048;
		let heap[0] = 0;
		let heap[1] = 14355;
	}
	function int peek(int address) {

	}

	function void poke(int address, int value) {

	}

	/**
	 * Heap management
	 */

	// Finds and allocates from the heap a memory block of the specified size and returns a reference to the base address
	function Array alloc(int size) {
		// search freeList using best-fit or first-fit heuristics to obtain a segment with segment size >= size + 2
		// if no such segment is found, return failure or attempt defrag
		// block = base address of the found space
		// update the freeList and the fields of block to account for the allocation
		// return block
	}

	// Deallocates the given object and frees its space
	// Called in dispose() method, for example. This is what garbage collectors in C# or Java do.
	function void deAlloc(Array o) {
		// append the object/block to the free list
	}

	function void deFrag() {

	}
}